# Section 3c — Proposed Traceability Matrix Updates (v0.6)

**Source section:** Engineering Specification — Section 3c: Model Layer and MLflow (approved as v1.0 LOCKED / APPROVED, 2026-04-30)
**Target file:** `docs/traceability_matrix.md`
**Action:** Replace the rows for Decisions 1, 2, 3, 4, 5, 6, 7, 11, 12, 16, and 17 with the rows below; transition Decisions 5, 6, 7, and 12 statuses where indicated. Decisions 1, 2, 3, 4, 11, 16, and 17 retain their existing status (`in spec`) with row text extended to record 03c's specific contributions or downstream-consumer references. Decision 5 status form evolves to record 03c's `rank_method` enumeration closing the Section 2 `pending_section_3` sentinel. Decision 6 transitions further within its existing split status: combined-score formula and ranking integration are now `in spec` (03c) for the 2-component first testable variant; activation of the reserved 4-component variant remains `pending`. Decision 7 status moves the calibration portion to `in spec` (03c); the walk-forward harness portion remains `pending` (Section 4). Decision 12 transitions from `pending` to a split status: model-state lifecycle schema and audit are `in spec` (03c, including the first promotion gate); second promotion gate, kill switch, and Active → Warning trigger remain `pending` (Section 5 plus an Open Question for the trigger definition).

The format and column headers match the existing `docs/traceability_matrix.md` table after the Section 3b lock merge.

**v0.6 of this companion file** corresponds to the v1.0 LOCKED / APPROVED version of Section 3c. There is one version of this companion artifact; the "v0.6" suffix follows the Section 2 / Section 3a / Section 3b companion convention of versioning the companion file independently of the matrix file (and matches the matrix-file version after the Section 3c lock merge).

---

## Replacement rows

| SDR Decision | Spec Section(s) | Module(s) | Config File(s) | Required Tests | Approval Gate | Status |
|---|---|---|---|---|---|---|
| Decision 1 — Phase 1 scope (ETF tactical research, no live trading) | 01 — Architecture Overview (no-live-broker boundary, scope reservations); 02 — Data Layer (EODHD-only adapter; no holdings/fundamentals/news/earnings/options ingestion); 03a — Feature Engineering (no fundamentals, holdings, news, earnings, options, individual stocks, autonomous research agents, or commercial features in any feature definition); 03b — Target Generation (no fundamentals, holdings, news, earnings, options, individual stocks, autonomous research agents, or commercial features in any target definition; the closed-set target-family enumeration in `config/targets.yaml` excludes all out-of-scope inputs); **03c — Model Layer (no fundamentals, holdings, news, earnings, options, individual stocks, Danelfin, autonomous research agents, or commercial features in any `model_kind`, calibration method, combined-score component, `rank_method` value, or YAML config in `config/model.yaml`; the closed `model_kind` enumeration on `models.model_definitions` and the closed `combined_score.formula_version` enumeration in `config/model.yaml` exclude all out-of-scope inputs)**; also touched by 05 (paper-only enforcement) and 06 (UI read-only) | Cross-cutting; enforced architecturally by the absence of broker SDKs, the structural exclusion of out-of-scope target inputs from `targets/`, and the structural exclusion of out-of-scope model inputs from `models/` | n/a at the architectural level; `config/targets.yaml` family enumeration is closed-set and excludes out-of-scope inputs (Section 3b); **`config/model.yaml` `model_kind`, `combined_score.components`, and `rank_method` enumerations are closed-set and exclude out-of-scope inputs (Section 3c)** | No-live-broker boundary test (multi-part); secrets-in-diff check; `config/features.yaml` validation tests reject unknown family keys (Section 3a test 8.2 #24); `config/targets.yaml` validation tests reject unknown family keys (Section 3b test 8.2 #24); **Closed-enum config-validation: `rank_method` rejection (Section 3c test RNK-01); rejection of unknown `model_kind`, unknown `calibration_method`, and unknown `combined_score.formula_version` values is by config-validation per 03c §7.3 (no separate §8 test enumerated); models/ has no provider/SDK imports (Section 3c test CC-05); no-Danelfin / no-individual-stock import-boundary at the `models/` package level is architecturally enforced per 03c §5.1 / §5.2 (no separate §8 test enumerated)** | Approver per Approval Matrix | in spec |
| Decision 2 — Data Provider and Provider-Switching Strategy | 01 — Architecture Overview (provider-abstraction boundary); 02 — Data Layer (provider interface methods, EODHD implementation, ingestion persistence with chunk-level atomicity, 30-day purge, raw payload secrets redaction); 03a — Feature Engineering (no `features/` module imports from `providers/` or any provider-specific library; adjusted-close is the canonical research price; no use of raw OHLCV in any feature); 03b — Target Generation (no `targets/` module imports from `providers/` or any provider-specific library; all target inputs flow through Postgres; adjusted-close is the canonical research price; no use of raw OHLCV in any target calculation); **03c — Model Layer (no `models/` module imports from `providers/` or any provider-specific library; all model inputs flow through Postgres via the `features.feature_values` and `targets.target_values` surfaces; 03c does not read directly from `prices.*` or any provider table; the model-fitting orchestrator does not require provider credentials)** | `providers/`, `providers/eodhd/`, `ingestion/`, `common/db_migrations.py`, `prices.etf_prices_daily`, `prices.corporate_actions`, `ops.provider_raw_payloads`, `ops.data_quality_exceptions`, `universe.etf_provider_mapping` (Section 2 schema); `features/` calculator modules (Section 3a); `targets/` calculator modules (Section 3b); **`models/` orchestrator and calculator modules (Section 3c)** | `config/universe.yaml` (per-ETF provider mapping); `config/features.yaml` (Section 3a); `config/targets.yaml` (Section 3b); **`config/model.yaml` (Section 3c)** | Provider abstraction import-boundary test (Section 1); provider interface signature tests (Section 2); EODHD provider tests (Section 2); ProviderBatchResult shape and per-symbol failure collection tests (Section 2); ingestion idempotency tests (Section 2); chunk-level atomicity test (Section 2); partial-run status test (Section 2); failed-run status test (Section 2); conflicting-provider-data fail-severity test (Section 2); 30-day deletion test (Section 2); raw payload secrets redaction test (Section 2); ingestion failure-mode tests (Section 2); no-provider-import test for `features/` (Section 3a test 8.2 #18); adjusted-close-only test for `features/` (Section 3a test 8.2 #19); no-provider-import test for `targets/` (Section 3b test 8.2 #18); adjusted-close-only test for `targets/` (Section 3b test 8.2 #19); **no-provider-import test for `models/` (Section 3c test CC-05); no-direct-`prices.*`-read at `models/` is architectural per 03c §5.2 (no separate §8 test enumerated); orchestrator does not require provider credentials per 03c §7.1 (no separate §8 test enumerated)** | Approver per Approval Matrix | in spec |
| Decision 3 — ETF Universe and Eligibility Rules | 02 — Data Layer (universe-layer model in schema and YAML; eligibility rules baseline; ETF-to-layer assignment; data-freshness thresholds in YAML); 03a — Feature Engineering (eligibility-row omission contract: `features.feature_values` rows are written only for ETF/date pairs that are rank-eligible per `universe.etf_eligibility_history` as of signal date `T`); 03b — Target Generation (parallel eligibility-row omission contract on `targets.target_values`: rows are written only for ETF/date pairs that are rank-eligible at signal date `T`, lie within ETF lifecycle bounds, are not delisted before the required `exit_date`, and whose required forward window does not exceed the snapshot's `price_table_max_as_of_date`); **03c — Model Layer (consumes the eligibility-row omission contract through the `features.feature_values` surface and the `targets.target_values` surface — both filtered on `status='succeeded'` — and does not re-derive eligibility; the absence of a row in either upstream surface is the consumption-side signal that the (etf, as_of_date) pair is ineligible / out-of-lifecycle / out-of-snapshot-coverage; predictions are emitted for the entire `(etf_id, as_of_date)` set in the succeeded feature run with `prediction_context` tagged per §6.2; combined-score / ranking outputs are filtered to rank-eligible ETFs at the relevant signal date via `universe.etf_eligibility_history`)** | `universe.etfs`, `universe.etf_eligibility_history`, `universe.sleeves`, `universe.benchmarks` schema (Section 2 migration `0001_*`); `ingestion/` universe loader; `common/` config loader; `features/` calculator modules consuming the as-of-date eligibility view (Section 3a); `targets/` calculator modules consuming the as-of-date eligibility view and the snapshot's `price_table_max_as_of_date` (Section 3b); **`models/` orchestrator and scoring modules consuming `features.feature_values`, `targets.target_values`, and `universe.etf_eligibility_history` (Section 3c)** | `config/universe.yaml` (incl. `data_freshness:` block, `disclosures.current_survivor_label`); `config/features.yaml` (Section 3a); `config/targets.yaml` (Section 3b); **`config/model.yaml` (Section 3c — `sleeve_rank_methods` block must enumerate every `sleeve_code` present in `universe.sleeves` per §7.3 rule 4)** | `config/universe.yaml` validation tests (Section 2); universe-layer enforcement test (Section 2); eligibility-rules check tests (Section 2); seeded sleeves and benchmarks present after initial migration (Section 2); survivorship-and-lifecycle test 3(c) per feature family asserting no `features.feature_values` row is written for ineligible pairs (Section 3a test 8.1 #3); survivorship-and-lifecycle tests per target family asserting no `targets.target_values` row is written for ineligible pairs (Section 3b test 8.1 #3); forward-window observability gate test asserting no row written when the required forward window exceeds snapshot coverage (Section 3b test 8.1 #5); **eligibility-row inheritance for 03c is architectural — the model orchestrator does not synthesize predictions for ETF/date pairs absent from the upstream surfaces (per 03c §5.2 / §6.1; no separate §8 test enumerated); sleeve-rank-method completeness test asserting `sleeve_rank_methods` covers every active sleeve (Section 3c test RNK-02); ranking-output filter to rank-eligible ETFs is architectural per 03c §6.7 / §6.11 (no separate §8 test enumerated)** | Approver per Approval Matrix | in spec |
| Decision 4 — Universe Survivorship and ETF Launch-Date Handling | 02 — Data Layer (universe-side schema, append-only eligibility history, current-survivor disclosure label storage, no-overlap constraints, canonical/denormalized field consistency rules); 03a — Feature Engineering (no `features.feature_values` row written for `(e, T)` where `T < first_traded_date(e)` or `T >= delisted_date(e)`); 03b — Target Generation (parallel: no `targets.target_values` row written for `(e, T)` where `T < first_traded_date(e)`, `T >= delisted_date(e)`, or where the ETF is delisted before the required `exit_date(e, T, h)` per the Approver-resolved `forward_window.delisted_within_window_rule = "no_row"`; replacement-ETF semantics not consulted by 03b — replacement remains a Section 5 portfolio concern); **03c — Model Layer (does not consult lifecycle dates directly; consumes upstream-enforced lifecycle bounds through the `features.feature_values` and `targets.target_values` surfaces and through `universe.etf_eligibility_history`; does not consult `replacement_etf_id` — replacement remains a Section 5 portfolio concern); also touched by Sections 4 and 6 for downstream enforcement and disclosure surfacing | `universe.etfs` lifecycle columns; `universe.etf_eligibility_history` (append-only, EXCLUDE constraint); `universe.etf_ticker_history` (EXCLUDE constraint); `universe.etf_provider_mapping` (EXCLUDE constraint); `ingestion/` enforcement; `common.get_current_survivor_label()` (Section 2); `features/` calculator modules consulting `first_traded_date` and `delisted_date` (Section 3a); `targets/` calculator modules consulting `first_traded_date` and `delisted_date` for both signal-date floor and forward-window-end checks (Section 3b); **`models/` orchestrator consuming the upstream-enforced lifecycle bounds without re-deriving them (Section 3c)** | `config/universe.yaml` (lifecycle fields seeded in initial config; `disclosures.current_survivor_label` string); `config/targets.yaml` (`forward_window.delisted_within_window_rule = "no_row"`); **`config/model.yaml` (Section 3c — no lifecycle-related parameter; 03c inherits 03a/03b's lifecycle gating)** | Point-in-time reconstruction tests (Section 2); no-overlap constraint tests (Section 2); canonical/denormalized field consistency tests (Section 2); rank-eligibility-before-`eligible_start_date` test (Section 2); ticker-change-via-`etf_id`-join test (Section 2); current-survivor disclosure label retrieval test (Section 2; semantic verification in Sections 4 and 6); survivorship-and-lifecycle tests 3(a) and 3(b) per feature family (Section 3a test 8.1 #3); survivorship-and-lifecycle tests per target family (Section 3b test 8.1 #3 and #6); **lifecycle-bounds inheritance for 03c is architectural — the model orchestrator does not synthesize predictions for ETF/date pairs absent from upstream surfaces, including pre-`first_traded_date`, on-or-after-`delisted_date`, and delisted-before-`exit_date` cases (per 03c §5.2 / §6.1; no separate §8 test enumerated)** | Approver per Approval Matrix | in spec |
| Decision 5 — Benchmark, Sleeve, and Diversifier Treatment | 02 — Data Layer (universe-side schema fields and seeded sleeves/benchmarks); 03a — Feature Engineering (benchmark-relative excess-return feature family — Family 4 — using `universe.etfs.primary_benchmark_id`; interim ETF-backed-benchmark constraint with `severity='warning'` `features.feature_run_issues` row on index-only-benchmark conditions); 03b — Target Generation (regression excess-return target family using the same `primary_benchmark_id` resolution; inherited interim ETF-backed-benchmark constraint with `severity='warning'` `targets.target_run_issues` row on index-only-benchmark conditions; regression target row is written with `target_value = NULL` in the index-only case, and the classification target row is also written with `target_value = NULL` by null propagation; no silent benchmark substitution; no fallback to `secondary_benchmark_id` in current 03b scope; sleeve assignments read but not modified by 03b; sleeve-conditional target families deferred to future 03b amendments); **03c — Model Layer (closed `rank_method` enumeration `('benchmark_relative', 'peer_relative', 'absolute_with_context')` per §6.7, closing the Section 2 `pending_section_3` sentinel obligation; per-sleeve mapping per §6.7 / §10.5: `BroadEquity → benchmark_relative`, `Sector → benchmark_relative`, `Thematic → benchmark_relative`, `BondTreasury → benchmark_relative`, `DiversifierHedge → absolute_with_context`, `REITRealAsset → peer_relative`; sleeve-aware combined-score and ranking math per §6.8 / §6.11 — component z-scores are pooled across all rank-eligible ETFs at signal date `T` for `benchmark_relative`, within sleeve at `T` for `peer_relative`, and not enabled for `absolute_with_context` under the current formula; ranking is materialized within sleeve in a separate step; no fallback to `secondary_benchmark_id` at the model layer; DiversifierHedge `absolute_with_context` is **not rank-enabled** under the current 2-component formula — those ETFs produce no `models.combined_scores` rows and one `severity='warning'` `models.scoring_run_issues` row per scoring run with `issue_type='rank_method_unsupported_in_phase1'`; activation requires an Approver-approved 03c amendment that adds absolute-trend / absolute-gain `model_kind` values and the absolute / risk inputs to an extended formula)** | `universe.etfs` (sleeve and benchmark columns); `universe.sleeves`; `universe.benchmarks` (Section 2 migration `0001_*`); `features/` benchmark-relative calculator (Section 3a); `targets/` regression excess-return calculator (Section 3b); **`models/` ranking module, `models/` `rank_method` validator, `models/` scoring module (Section 3c); `models.scoring_runs`, `models.scoring_run_models`, `models.combined_scores`, `models.scoring_run_issues` schema (Section 3c)** | `config/universe.yaml` (sleeve and benchmark definitions; per-ETF assignments; `rank_method` string per ETF); `config/features.yaml` (`benchmark_relative.benchmark_field: primary_benchmark_id`; `benchmark_relative.require_etf_backed_benchmark: true`); `config/targets.yaml` (`regression_excess_return.benchmark_field: primary_benchmark_id`; `regression_excess_return.require_etf_backed_benchmark: true`); **`config/model.yaml` (`allowed_rank_methods` closed enum; `sleeve_rank_methods` per-sleeve mapping; `combined_score.formula_version` closed enum)** | `config/universe.yaml` validation tests (Section 2); seeded sleeves and benchmarks present after initial migration (Section 2); per-ETF sleeve and benchmark assignment validation (Section 2); `rank_method` presence-and-type validation (Section 2); Family 4 known-input / known-output, T-1 alignment, ETF-backed-benchmark resolution, index-only-benchmark behavior (two-part), no-fallback-to-secondary-benchmark, non-overlapping trading history tests (Section 3a tests 8.1 Family 4 #10–#14); regression excess-return known-input / known-output, forward Convention B alignment, ETF-backed-benchmark resolution, index-only-benchmark behavior (two-part), no-fallback-to-secondary-benchmark, forward cross-series alignment for excess-return targets tests (Section 3b tests 8.1 #1–#7 and 8.2 #33); **`rank_method` closed-enum validation test (Section 3c test RNK-01); `pending_section_3` sentinel rejection by `config/model.yaml` semantic validation per 03c §7.3 rule 5 (additionally closed by RNK-02 sleeve-completeness check); per-sleeve mapping completeness test (Section 3c test RNK-02); rank ordering within sleeve test (Section 3c test RNK-04); cross-sectional z-scoring per `(signal_date, horizon)` test (Section 3c test CS-02); DiversifierHedge no-`combined_scores`-rows behavior under the current formula is architectural per 03c §6.7 / §10.6 (no separate §8 test enumerated); DiversifierHedge `severity='warning'` `scoring_run_issues` row per scoring run is architectural per 03c §6.7 / §10.6 (no separate §8 test enumerated); no-fallback-to-`secondary_benchmark_id` static check at `models/` (Section 3c test CC-07)** | Approver per Approval Matrix | in spec (universe-side, benchmark-relative feature side, benchmark-relative target side, **and 03c `rank_method` enumeration + sleeve-aware ranking math**); pending (DiversifierHedge `absolute_with_context` activation — requires future 03c amendment) |
| Decision 6 — Target design (regression + calibrated probability) and combined score philosophy | 03a — Feature Engineering (lookback windows aligned to support 63 and 126 forward-trading-day target horizons; 03a does not generate target labels); 03b — Target Generation cashes out the regression excess-return and classification outperformance families at exactly 63 and 126 trading-day horizons (Approver-resolved per §11.6 #8 and §11.6 #9), the entry/exit Convention B (§11.6 #1, entry `T+1`, exit `T+1+h`), the classification threshold `θ = 0.0` with strict positive excess return (§11.6 #2), the missing-forward-data rule `null_on_any_missing_input_in_window` (§11.6 #3), the ETF-delisted-within-window rule `no_row` (§11.6 #4), the snapshot-coverage / front-edge-truncation absence pattern (§11.6 #10), the target persistence model with `data_snapshot_id` linkage (§11.6 #5), the index-only benchmark interim behavior (§11.6 #6), the forward-data-only labeling, and the per-row window metadata (`signal_date`, `horizon_trading_days`, `entry_date`, `exit_date`) Section 4's purge/embargo logic consumes; **03c — Model Layer fits the dual-target framework (regression baseline = ridge regression on standardized features for `excess_return_h63` and `excess_return_h126`; classification baseline = logistic regression with held-out calibration for `outperformance_h63` and `outperformance_h126` — both Proposed defaults requiring approval at first build); produces the first testable combined-score formula = `w_reg * z(predicted_excess_return_h) + w_cls * z(calibrated_probability_outperformance_h)` with weights summing to `1.0 ± 1e-6` and Proposed defaults `w_reg = 0.5`, `w_cls = 0.5`, computed per `(etf, signal_date, horizon)` with component z-scores over the rank-method-defined ranking cross-section per §6.8 (pooled across all rank-eligible ETFs for `benchmark_relative`; within sleeve for `peer_relative`; not enabled for `absolute_with_context`); reserves the 4-component variant (risk score + trend / relative-strength confirmation per Decision 6) in `config/model.yaml` `combined_score.formula_version` enum but does not enable it in current 03c scope (activation requires Approver-approved 03c amendment); ranking materialized within sleeve)** | `features/` returns/momentum and benchmark-relative calculators feeding 03b's target generation (Section 3a); `targets/` regression and classification calculators (Section 3b); `targets.target_runs`, `targets.target_definitions`, `targets.target_values`, `targets.target_run_issues` schema (Section 3b); **`models/` baseline regression module, `models/` baseline classification module, `models/` calibration module, `models/` scoring module (Section 3c); `models.model_definitions`, `models.model_runs`, `models.model_predictions`, `models.scoring_runs`, `models.scoring_run_models`, `models.combined_scores`, `models.model_run_issues`, `models.scoring_run_issues` schema (Section 3c)** | `config/features.yaml` (window choices: `returns_momentum.windows_trading_days: [21, 63, 126, 252]`; `benchmark_relative.windows_trading_days: [63, 126]`); `config/targets.yaml` (`families.regression_excess_return.horizons_trading_days: [63, 126]`; `families.classification_outperformance.horizons_trading_days: [63, 126]`; `families.classification_outperformance.threshold: 0.0`; `alignment.convention: "B"`; `forward_window.delisted_within_window_rule: "no_row"`; `missing_data.rule: "null_on_any_missing_input_in_window"`); **`config/model.yaml` (`allowed_model_kinds` closed enum; `combined_score.formula_version` closed enum; `combined_score.components` weight table; `training.min_training_rows` floor; `training.random_seed`; `model_set_version` discipline)** | Family 1 and Family 4 known-input / known-output, T-1 alignment, idempotency tests at 63/126-trading-day windows (Section 3a tests 8.1); regression and classification target known-input / known-output, forward Convention B alignment, no-T-1-leakage, idempotency at 63/126-trading-day horizons (Section 3b tests 8.1); blocked-run lifecycle tests for invalidated snapshot, failed ingestion run, partial ingestion run (Section 3b tests 8.2 #21, #22); coverage-parity-with-03a test (Section 3b test 8.2 #37); **training-data-assembly correctness tests including `(etf_id, as_of_date)` join with both upstream filters and front-edge horizon handling (Section 3c tests TDA-01..TDA-07); regression and classification baseline fitting tests covering shape consistency, deterministic reproducibility, standardization-parameter persistence, and graceful failure on insufficient training data (Section 3c tests MOD-01..MOD-04); calibration pipeline tests covering output range, held-out-fold discipline, parent-uncalibrated FK, and config-validation rejection of orphaned calibrators (Section 3c tests CAL-01..CAL-04); combined-score formation tests including z-scoring per `(signal_date, horizon)` cross-section (Section 3c test CS-02); CS-01 succeeded-only-component-runs test (Section 3c); weight-sum-constraint test (Section 3c test CS-03); sleeve-aware ranking math tests covering closed-enum rejection, sleeve completeness, multi-sleeve eligibility, and within-sleeve ranking order (Section 3c tests RNK-01..RNK-04)**; walk-forward harness tests (Section 4, **pending**) | Approver per Approval Matrix | **`in spec` (regression and classification target generation, label alignment, per-row window metadata, persistence with `data_snapshot_id` linkage, **dual-target baseline implementations, calibration pipeline, first testable 2-component combined-score formula, sleeve-aware ranking math**); `pending` (4-component formula activation; walk-forward harness — Section 4)** |
| Decision 7 — Validation method (walk-forward with 126-day purge/embargo) | 03a — Feature Engineering (T-1 trading-day alignment on every feature row supports later walk-forward harness construction without bias); 03b — Target Generation (per-row window metadata `signal_date`, `horizon_trading_days`, `entry_date`, `exit_date` recorded on every `targets.target_values` row so Section 4's walk-forward harness applies purge/embargo at train/test boundaries without re-deriving windows from horizon arithmetic; overlapping-label structure recorded explicitly via the per-row window columns; 03b does NOT apply purge/embargo at the target-emission level — Section 4 owns that); **03c — Model Layer (calibration pipeline per the SDR's named methods, with closed enumeration `('platt', 'isotonic', 'logistic_on_folds')` on `models.model_definitions.calibration_method`; held-out calibration over Section-4-supplied fold structure through the §12.1 invocation seam; 03c stores `prediction_context` per row on `models.model_predictions` with closed enumeration `('in_sample_diagnostic', 'walk_forward_oos', 'current_scoring')` so Section 4 can communicate fold-aware OOS-vs-in-sample to downstream consumers; 03c does NOT construct folds, does NOT select embargo width, and does NOT enforce purge boundaries at emission time — Section 4 owns those)**; 04 — Backtest / Attribution / Validation (walk-forward harness, purge/embargo enforcement, leakage tests, **pending**) | `features/` calculator modules establishing T-1 alignment (Section 3a); `targets/` calculator modules establishing forward Convention B alignment and recording per-row window metadata (Section 3b); **`models/` calibration module and `models/` orchestrator setting `prediction_context` (Section 3c); `models.model_predictions.prediction_context` column (Section 3c)**; `backtest/`, `attribution/` modules (Section 4, **pending**) | `config/features.yaml` (T-1 alignment is structural, not parameterized); `config/targets.yaml` (forward Convention B alignment is structural; horizons parameterized at 63 and 126 trading days); **`config/model.yaml` (`allowed_calibration_methods` closed enum; per-`model_kind` calibration method assignment on `models.model_definitions`)**; backtest-window and validation parameters (Section 4, **pending**) | T-1 trading-day alignment tests per feature family (Section 3a tests 8.2 #31, #32, #33); forward Convention B alignment tests per target family (Section 3b tests 8.2 #31, #32, #33); adjacent-target-rows-with-overlapping-forward-windows expected (Section 3b §9 edge case 16); **calibration correctness tests covering output range, held-out-fold discipline, parent-uncalibrated FK, and config-validation rejection of orphaned calibrators (Section 3c tests CAL-01..CAL-04); calibration-method closed-enum validation by `config/model.yaml` semantic validation per 03c §7.3 (no separate §8 test enumerated); `prediction_context` closed-enum validation test (Section 3c test DC-09); `prediction_context` emission rule under non-Section-4 invocation is architectural per 03c §6.2 (no separate §8 test enumerated)**; walk-forward harness, purge/embargo, leakage tests (Section 4, **pending**) | Approver per Approval Matrix | **`in spec` (calibration pipeline, `prediction_context` storage column — 03c)**; `pending` (walk-forward harness, purge/embargo enforcement, leakage tests — Section 4) |
| Decision 11 — Model Tracking, Attribution, Data Quality, and Lightweight MLOps | 01 — Architecture Overview (Postgres-as-system-of-record boundary; MLflow-as-tracking architecture); 02 — Data Layer (data-quality framework with framework-level enforcement of forbidden auto-resolutions; `ingestion/` is the only emitter of `ops.data_quality_exceptions` rows in Phase 1; framework remains ingestion-owned); 03a — Feature Engineering (`features.feature_runs.data_snapshot_id` linkage; `features.feature_values.feature_run_id` row-level traceability via composite FK; `features.feature_run_issues` as the feature-layer issue log with closed `issue_type` and `severity` enumerations and the open-run-before-validation lifecycle); 03b — Target Generation (`targets.target_runs.data_snapshot_id` linkage; `targets.target_values.target_run_id` row-level traceability via composite FK; `targets.target_run_issues` as the target-layer issue log with closed `issue_type` and `severity` enumerations and the open-run-before-validation lifecycle (the closed `issue_type` enumeration does **not** include a `forward_window`-related value in current 03b scope per §11.6 #10); database-level `target_set_version` integrity via UNIQUE on `target_runs(target_run_id, target_set_version)` plus composite FKs from `target_values`); `ops.data_quality_exceptions` remains ingestion-owned and unmodified by 03b per Section 2 v1.0 LOCKED; **03c — Model Layer (MLflow client-side integration on writer side, with one MLflow run per `models.model_runs` row (1:1) and one MLflow registered model + version per `models.model_versions` row (1:1); MLflow runs link back to BOTH `feature_run_id` AND `target_run_id` via tags and Postgres FKs on `models.model_runs`; MLflow tags include the closed required tag set defined in 03c §6.10; `models.model_runs.data_snapshot_id` redundant FK for query convenience; orchestrator asserts feature_run and target_run reference the same `data_snapshot_id`; `models.model_run_issues` and `models.scoring_run_issues` as the model-layer issue logs with **disjoint** closed `issue_type` enumerations per §6.6 and the same closed two-value `severity` enumeration `('warning', 'fail')` and the same open-run-before-validation lifecycle; `ops.data_quality_exceptions` remains ingestion-owned and unmodified by 03c per Section 2 v1.0 LOCKED; database-level composite-FK integrity on `models.model_runs(feature_run_id, feature_set_version)` and `(target_run_id, target_set_version)`)**; 04 — Backtest / Attribution (attribution storage; data-snapshot consumption, **pending**) | `common/` (db helpers, mlflow helpers); `ingestion/` (data-quality emitter); `ops.data_quality_exceptions`, `ops.ingestion_runs`, `ops.provider_raw_payloads`, `ops.data_snapshots`, `ops.schema_migrations` (Section 2 schema); `scripts/postgres-init/`; `features.feature_runs`, `features.feature_definitions`, `features.feature_values`, `features.feature_run_issues` (Section 3a schema); `targets.target_runs`, `targets.target_definitions`, `targets.target_values`, `targets.target_run_issues` (Section 3b schema); **`models.model_definitions`, `models.model_runs`, `models.model_predictions`, `models.model_run_issues`, `models.scoring_runs`, `models.scoring_run_models`, `models.combined_scores`, `models.scoring_run_issues`, `models.model_versions`, `models.model_state_history` (Section 3c schema); `models/` MLflow writer, `models/` orchestrator, `models/` scoring orchestrator (Section 3c)**; `backtest/`, `attribution/` MLflow writers (later-section, **pending**); `ui/` Model Registry / Run Browser (Section 6 reader, **pending**) | n/a at the architectural level; `.env` for MLflow connection (Section 2; **03c directly reads only `MLFLOW_TRACKING_URI` and inherits application-DB access from Section 2 — no new env vars introduced by 03c**); `config/features.yaml` (Section 3a); `config/targets.yaml` (Section 3b); **`config/model.yaml` (Section 3c — `model_set_version` discipline; `mlflow.required_tags` closed set; `mlflow.experiment_name` per `model_set_version`)** | Postgres-as-system-of-record import test (Section 1); cross-container connectivity smoke tests (Section 1); database/role bootstrap test (Section 2); migration-runner refusal of `CREATE DATABASE` / `CREATE ROLE` test (Section 2); data-quality framework tests covering nine-field exception report and forbidden auto-resolutions (Section 2); ingestion-run metadata test (Section 2); data snapshot creation and invalidation tests (Section 2); 03a/03b parallel `data_snapshot_id` linkage, invalidated-snapshot rejection, ingestion-run consumption, failed-run exclusion, set-version consistency (database-level), composite FK to definitions, no-write-to-`ops.*` static check, run_issues write/query, cross-run idempotency tests (Section 3a/3b tests 8.2); **parallel `models.*` tests: `data_snapshot_id` linkage and snapshot-chain mismatch handling per 03c §6.12 / §9.2; `feature_set_version` / `target_set_version` consistency at the database level via composite FK (Section 3c test DC-01); training-data-assembly composite FK enforcement (Section 3c test TDA-06); snapshot consistency across `feature_run_id` and `target_run_id` (Section 3c test TDA-07); no-write-to-`ops.*` / `features.*` / `targets.*` / `universe.*` / `prices.*` static checks (Section 3c tests CC-03 and CC-04); `models.model_run_issues.issue_type` closed-enum (Section 3c test DC-02); `models.scoring_run_issues.issue_type` closed-enum (Section 3c test DC-07); `models.scoring_run_issues.severity` closed-enum (Section 3c test DC-08); deterministic reproducibility under fixed seed (Section 3c test MOD-02); MLflow writer-side tests covering 1:1 mapping with `feature_run_id` AND `target_run_id` tag presence (Section 3c test MLF-01), required-tags-present check against the §6.10 closed tag set (Section 3c test MLF-02), MLflow connectivity-failure handling (Section 3c test MLF-03), and fitted-model artifact serialization round-trip (Section 3c test MLF-04); error-path coverage via TDA-02 / TDA-03 / TDA-04 (failed upstream blocked), MOD-04 (insufficient training data), CAL-04 (orphan calibrator rejected at config-validation), CS-03 (weight-sum mismatch rejected at config-validation), and DC-04 (`mlflow_model_uri`-on-Active CHECK)** | Approver per Approval Matrix | in spec |
| Decision 12 — Two-gate model promotion + multi-condition kill switch | **03c — Model Layer (`models.model_versions` schema with closed-enum `state` field `('Active', 'Warning', 'Paused', 'Retired')` per SDR Decision 12; `models.model_state_history` append-only audit table recording every state transition; partial unique active-version index `(model_kind) WHERE state='Active'` ensuring at most one Active version per `model_kind`; first promotion gate = registration of a `models.model_versions` row at the Proposed-default initial state `Paused`; Proposed defaults requiring Approver approval at first build per §11.5 — `initial_state_on_register: Paused`, `approver_only_transitions: true`, `retired_is_terminal: true`; SDR Decision 12 names the four states but does not specify initial state, terminality of `Retired`, or whether all transitions are Approver-only in Phase 1; `mlflow_model_uri` required on every `state='Active'` row via CHECK constraint; one MLflow registered model + version per `models.model_versions` row); 05 — Portfolio Layer (second promotion gate — when an Active version is consumed by the paper portfolio; kill switch integrated with `models.model_versions` lifecycle subject to Approver-only-Phase-1 transition default; **pending**); 03c §10.9 Open Question carried forward — Active → Warning trigger definition (Section 5 may propose; adoption requires 03c amendment)** | **`models.model_versions`, `models.model_state_history` schema (Section 3c); `models/` model-version registration module, `models/` state-transition writer (Section 3c)**; `portfolio/` second-promotion-gate module, `portfolio/` kill-switch module (Section 5, **pending**); `ui/` Model Registry / Run Browser (Section 6 reader, **pending**) | **`config/model.yaml` (Section 3c — `model_state.initial_state_on_register`, `model_state.approver_only_transitions`, `model_state.retired_is_terminal`, `model_state.active_warning_trigger_definition: open_question`)**; `config/portfolio.yaml` (Section 5, **pending**) | **Model-state lifecycle tests covering initial state on register is Paused (Section 3c test STA-01), Active state requires approval metadata via CHECK constraint (Section 3c test STA-02), Retired-is-terminal enforcement (Section 3c test STA-03), Approver-only-transitions in Phase 1 (Section 3c test STA-04), and `mlflow_model_uri` non-null on every `Active` row via CHECK constraint (Section 3c test DC-04)**; second-promotion-gate tests, kill-switch tests (Section 5, **pending**) | Approver per Approval Matrix | **`in spec` (model-state lifecycle schema and audit, first promotion gate — 03c); `pending` (second promotion gate, kill switch — Section 5; Active → Warning trigger definition — Open Question §10.9 in 03c, may be proposed by Section 5)** |
| Decision 16 — Phase 1 Success Criteria and Bias Controls | 01 — Architecture Overview (architectural reservation only); 02 — Data Layer (universe time-awareness foundation, data-layer-level look-ahead and survivorship checks, data-snapshot foundation for reproducibility); 03a — Feature Engineering (T-1 trading-day alignment on every feature row including weekend / holiday / cross-series boundaries; survivorship gating producing absence of rows for ineligible / out-of-lifecycle ETF/date pairs; reproducibility via `data_snapshot_id` and `feature_run_id`; idempotency across `'succeeded'` runs; index-only-benchmark interim constraint preserves bias controls by refusing silent substitution); 03b — Target Generation (forward-data-only labeling on every target row via Convention B; survivorship gating producing absence of rows for ineligible / out-of-lifecycle ETF/date pairs and for ETFs delisted before required `exit_date`; row-level forward-window observability via the snapshot's `price_table_max_as_of_date` producing absence — not a failed run — for front-edge truncation; reproducibility via `targets.target_runs.data_snapshot_id` and `targets.target_values.target_run_id`; idempotency across `'succeeded'` runs sharing the same `data_snapshot_id` / `target_set_version` / `commit_hash` / `config_hash`; per-row window metadata supporting Section 4 purge/embargo enforcement; index-only-benchmark interim constraint preserves bias controls by refusing silent substitution and writing rows with `target_value = NULL` rather than fabricating values); **03c — Model Layer (`prediction_context` closed enumeration `('in_sample_diagnostic', 'walk_forward_oos', 'current_scoring')` on every `models.model_predictions` row preserves bias controls by tagging the operational regime in which each prediction was produced — Section 4 supplies fold metadata for `walk_forward_oos` tagging through the §12.1 invocation seam; reproducibility chain extended through MLflow runs linking back to BOTH `feature_run_id` AND `target_run_id` (and transitively to `data_snapshot_id`); idempotency across `'succeeded'` runs sharing the same `data_snapshot_id` / `feature_set_version` / `target_set_version` / `model_set_version` / `commit_hash` / `config_hash` / `random_seed`; index-only-benchmark interim constraint inherited unchanged — no `secondary_benchmark_id` fallback at the model layer; Section 4 walk-forward harness, purge / embargo enforcement, and OOS leakage tests **remain Section 4's**, NOT 03c's; survivorship and lifecycle bias controls inherited unchanged from 03a/03b through the eligibility-row omission contract)**; 04 — Backtest / Attribution (purge/embargo enforcement, leakage tests, reproducibility metadata, regime reporting, **pending**); 06 — UI (surface bias-control disclosures and labels including `prediction_context` and survivorship label, **pending**) | Cross-cutting; Section 2 owns `universe.etf_eligibility_history`, lifecycle columns, `prices.etf_prices_daily.adjusted_close` convention, `disclosures.current_survivor_label`, `ops.data_snapshots`, data-layer look-ahead checks; Section 3a owns the `features.*` schema and the feature-level T-1 / survivorship / reproducibility / idempotency tests; Section 3b owns the `targets.*` schema and the target-level forward-Convention-B / survivorship / forward-window-observability / reproducibility / idempotency tests; **Section 3c owns the `models.*` schema (ten tables: `model_definitions`, `model_runs`, `model_predictions`, `model_run_issues`, `scoring_runs`, `scoring_run_models`, `combined_scores`, `scoring_run_issues`, `model_versions`, `model_state_history`) and the model-level `prediction_context` / reproducibility-chain-extension / idempotency / no-`secondary_benchmark_id`-fallback / survivorship-inheritance tests**; Sections 4 and 6 own their respective downstream contributions | Cross-cutting; Section 2 owns `config/universe.yaml`; Section 3a owns `config/features.yaml`; Section 3b owns `config/targets.yaml`; **Section 3c owns `config/model.yaml` (closed-set enum validation; `model_set_version` discipline)** | Section 1 introduces no test for invariant 7. Section 2 introduces: point-in-time reconstruction tests; canonical/denormalized field consistency tests; no-overlap constraint tests on the three effective-dated tables; rank-eligibility-before-`eligible_start_date` test; current-survivor disclosure label retrieval test; data-layer look-ahead checks; data snapshot creation and invalidation tests. Section 3a introduces: per-feature-family T-1 alignment tests; per-feature-family survivorship-and-lifecycle tests; per-feature-family idempotency tests; data_snapshot/feature_set_version FK and consistency tests; cross-run idempotency test; feature_run_issues write/query (Section 3a tests 8.1 and 8.2). Section 3b introduces: per-target-family forward Convention B alignment tests; per-target-family survivorship-and-lifecycle tests including delisted-before-`exit_date`; per-target-family forward-window-observability tests; per-target-family idempotency tests; data_snapshot/target_set_version FK and consistency tests; cross-run idempotency test; target_run_issues write/query; coverage-parity-with-03a test (Section 3b tests 8.1 and 8.2). **Section 3c introduces: `prediction_context` closed-enum validation test (DC-09); `prediction_context` emission rule under non-Section-4 invocation is architectural per 03c §6.2 (no separate §8 test enumerated); per-`model_kind` reproducibility via MLflow `feature_run_id` AND `target_run_id` tag presence (Section 3c test MLF-01) plus required-tag-set check against §6.10 (Section 3c test MLF-02); cross-run idempotency under shared `data_snapshot_id` / `feature_set_version` / `target_set_version` / `model_set_version` / `commit_hash` / `config_hash` / `random_seed` is the deterministic-reproducibility property tested by MOD-02; no-`secondary_benchmark_id`-fallback static check at `models/` (Section 3c test CC-07); survivorship-inheritance — no synthesized predictions for ETF/date pairs absent from upstream surfaces — is architectural per 03c §5.2 / §6.1 (no separate §8 test enumerated); `models.model_run_issues` and `models.scoring_run_issues` write/query coverage with disjoint `issue_type` enumerations (Section 3c tests DC-02 and DC-07).** Section 4 introduces purge/embargo and leakage tests, **pending**. | Approver per Approval Matrix | in spec |
| Decision 17 — Phase 1 operator UI (Dash) on Linux/Hostinger VPS | 01 — Architecture Overview (UI as read-only architectural area, hosted inside the application container; UI read-only invariant covers both Python imports and database-access paths); **03c — Model Layer (downstream-consumer reference: the Section 6 UI Model Registry / Run Browser will read from the `models.*` tables 03c introduces — `models.model_versions`, `models.model_state_history`, `models.model_runs`, `models.model_predictions`, `models.scoring_runs`, `models.combined_scores`, `models.model_run_issues`, `models.scoring_run_issues` — and from the corresponding MLflow runs and registered models; 03c contributes the read surfaces, not the UI itself; UI-level enforcement of the read-only constraint at the database role level remains Section 6's; bias-control disclosures including `prediction_context` and the DiversifierHedge unranked-ETFs case must be surfaced by Section 6)**; 06 — UI Layer (seven Phase 1 screens, content, and the database-access enforcement mechanism for the read-only constraint, **pending**) | `ui/` (Section 6, **pending**); **`models.*` read surfaces (Section 3c)** | TBD (Section 6 will specify; UI screens read from existing `config/*.yaml` and may add UI-only config) | UI read-only import-boundary test (Section 1); UI database-access enforcement test — no INSERT/UPDATE/DELETE/DDL from `ui/` (Section 6, **pending**); UI smoke tests confirming pages load (Section 6, **pending**); UI cannot bypass approval gates or modify SDR-governed state (Section 6, **pending**); **UI read-only enforcement against `models.*` tables verified at the database role level (Section 6, **pending**)** | Approver per Approval Matrix | in spec |

---

## Audit findings (proposed corrections to existing matrix rows)

While preparing this update, the existing `docs/traceability_matrix.md` (post-Section 3b lock state, v0.5) was checked against the locked SDR v1.0, Section 1 v1.0 LOCKED, Section 2 v1.0 LOCKED, Section 3a v1.0 LOCKED / APPROVED, Section 3b v1.0 LOCKED / APPROVED, and Section 3c v1.0 LOCKED / APPROVED for label and status accuracy. **No new label findings** were identified.

The Decision 5 status form `in spec (universe-side, benchmark-relative feature side, and benchmark-relative target side); pending (ranking math, rank_method values)` evolves to `in spec (universe-side, benchmark-relative feature side, benchmark-relative target side, and 03c rank_method enumeration + sleeve-aware ranking math); pending (DiversifierHedge absolute_with_context activation — requires future 03c amendment)`. The `pending (ranking math, rank_method values)` portion of the v0.5 status form is now resolved by 03c's rank_method closed enumeration and sleeve-aware ranking math; the remaining `pending` element is narrower — only the activation of `absolute_with_context` ranking under an extended formula. Alternative split into 5a / 5b / 5c / 5d / 5e remains at Approver discretion; not proposed here.

The Decision 6 status form `in spec (regression and classification target generation, label alignment, per-row window metadata, persistence with data_snapshot_id linkage); pending (combined-score formula, ranking integration — 03c)` evolves further within its existing split status: dual-target baseline implementations, calibration pipeline, the first testable 2-component combined-score formula, and sleeve-aware ranking math are now `in spec`; the 4-component formula activation and the walk-forward harness (Section 4) remain `pending`. The Decision 6 row text is the post-Section-3c-lock formulation, supersedes the Section 3b-lock placeholder language for the `pending (combined-score formula, ranking integration — 03c)` portion, and explicitly records that the 4-component variant remains reserved for future amendment.

The Decision 7 status moves from overall `pending` to a split status: `in spec (calibration pipeline, prediction_context storage column — 03c); pending (walk-forward harness, purge/embargo enforcement, leakage tests — Section 4)`. The 03c calibration pipeline portion is now `in spec`; the Section 4 walk-forward harness portion remains `pending`. The row text records 03c's per-row `prediction_context` storage column as the contract by which Section 4 communicates fold-aware OOS-vs-in-sample to downstream consumers.

The Decision 12 status moves from overall `pending` to a split status: `in spec (model-state lifecycle schema and audit, first promotion gate — 03c); pending (second promotion gate, kill switch — Section 5; Active → Warning trigger definition — Open Question §10.9 in 03c, may be proposed by Section 5)`. The 03c portion (model-state schema, the four-state closed enumeration, the audit table, the partial unique active-version index, the first promotion gate at registration) is now `in spec`; the Section 5 portion (second promotion gate, kill switch) remains `pending`. The Active → Warning trigger definition is preserved as an Open Question carried forward.

---

## Notes for the matrix maintainer (Approver)

- **Decision 5 status entry** evolves to `in spec (universe-side, benchmark-relative feature side, benchmark-relative target side, and 03c rank_method enumeration + sleeve-aware ranking math); pending (DiversifierHedge absolute_with_context activation — requires future 03c amendment)`. The `pending` element is narrower than the v0.5 form (no longer "ranking math, rank_method values" but only "DiversifierHedge absolute_with_context activation"). Alternative split into 5a / 5b / 5c / 5d / 5e is at Approver discretion; not proposed here.
- **Decision 6 status entry** transitions further within its existing split status: dual-target baseline implementations, calibration pipeline, first testable 2-component combined-score formula, and sleeve-aware ranking math are now `in spec`; the 4-component formula activation and the Section 4 walk-forward harness remain `pending`.
- **Decision 7 status entry** moves to a split status. The 03c calibration pipeline portion is `in spec`; the Section 4 walk-forward harness portion is `pending`. After Section 4 approval, the `pending` portion will close.
- **Decision 11 row** continues to be `in spec` with row text extended to include 03c's MLflow writer-side integration, the ten-table `models.*` schema, and the disjoint-issue-table discipline. After Section 4 approval, consider adding Section 4 attribution storage; not proposed here.
- **Decision 12 row** (previously TBD across all columns under v0.5) is filled out for the first time by 03c. Status moves from overall `pending` to a split status: `in spec (model-state lifecycle schema and audit, first promotion gate — 03c); pending (second promotion gate, kill switch — Section 5; Active → Warning trigger definition — Open Question §10.9 in 03c)`. After Section 5 approval, the `pending` portion narrows further.
- **Decision 16 row** continues to expand as Section 4 and Section 6 add their respective tests. After Section 4 approval, consider a row split (16a data-layer auditability, 16b feature/target/model auditability, 16c backtest/attribution auditability, 16d UI disclosure surfacing). Premature now.
- **Decision 17 row** retains overall `in spec` status with row text extended to record 03c's downstream-consumer contribution (the `models.*` tables the Section 6 UI Model Registry / Run Browser will read). The UI itself remains Section 6's; 03c contributes the read surfaces.
- **Decisions 8, 9, 10, 13, 14, 15, 18** are not touched by Section 3c and require no row changes at the Section 3c lock merge. Their existing post-Section-3b-lock rows are preserved.
- **`models.*` schema as project artifact.** The ten tables (`models.model_definitions`, `models.model_runs`, `models.model_predictions`, `models.model_run_issues`, `models.scoring_runs`, `models.scoring_run_models`, `models.combined_scores`, `models.scoring_run_issues`, `models.model_versions`, `models.model_state_history`) are introduced by Section 3c and should be added to the matrix's project-artifacts cross-reference if such a cross-reference exists. The Section 3a and Section 3b companion files made the analogous notes for the `features.*` and `targets.*` schemas; the Section 2 companion file made the analogous note for `scripts/postgres-init/`.
- **`config/model.yaml`** is owned by Section 3c (the `model_set_version`-discipline file parallel to `config/features.yaml` and `config/targets.yaml`). The Section 1 *Config dependencies* table already enumerates `config/model.yaml`; no Section 1 amendment is proposed for the introduction of 03c's `config/model.yaml` content.
- **MLflow as project artifact.** MLflow is the experiment tracker (writer side introduced by 03c); MLflow tracking metadata is in Postgres via the separate `mlflow` database in the same Postgres cluster (per Section 2 v1.0 LOCKED) and MLflow artifacts are in the `mlflow-artifacts` Docker named volume (per Section 1 / Section 2). 03c does not introduce a new database; 03c writes to the existing MLflow stack established by Sections 1 and 2.

---

**End of proposed updates.**
