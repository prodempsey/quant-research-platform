# Quant Research Platform — Traceability Matrix

**Phase 1 scope:** ETF tactical research platform.
**Document status:** v0.5 (Sections 1, 2, 3a, and 3b v1.0 LOCKED merged; Sections 3c, 4, 5, 6 still pending)
**Date:** 2026-04-29
**Companion documents:**
- Quant Research Platform — Strategy Decision Record (Phase 1)
- Quant Research Platform — Engineering Workflow v1.5 (locked)
- Engineering Specification — Section 1 v1.0 LOCKED (`docs/engineering_spec/01_architecture_overview.md`)
- Engineering Specification — Section 2 v1.0 LOCKED (`docs/engineering_spec/02_data_layer.md`)
- Engineering Specification — Section 3a v1.0 LOCKED / APPROVED (`docs/engineering_spec/03a_feature_engineering.md`)
- Engineering Specification — Section 3b v1.0 LOCKED / APPROVED (`docs/engineering_spec/03b_target_generation.md`)

**Purpose:** Map each SDR decision to its downstream artifacts — Engineering Specification section, implementing modules, configuration files, required tests, and approval gates. This is the single place to verify that every strategic decision has corresponding implementation, configuration, test coverage, and approval discipline.

**Maintenance:** Per Engineering Workflow Section 3.6, this matrix is updated as part of finalizing each Engineering Specification section. A spec section cannot be finalized while its matrix entry is missing or stale. When the SDR is revised, this matrix is the entry point for the impact assessment described in Engineering Workflow Section 6.

**Status legend:**
- `pending` — Not yet covered by an approved spec section
- `in spec` — Spec section drafted but not yet approved, or spec section approved and modules not yet started
- `in build` — Spec section approved, modules being implemented
- `live` — Modules merged and integrated
- `n/a` — Decision does not require module-level implementation (e.g., a strategy commitment that constrains future work but generates no code)

---

## Matrix

| SDR Decision | Spec Section(s) | Module(s) | Config File(s) | Required Tests | Approval Gate | Status |
|---|---|---|---|---|---|---|
| Decision 1 — Phase 1 scope (ETF tactical research, no live trading) | 01 — Architecture Overview (no-live-broker boundary, scope reservations); 02 — Data Layer (EODHD-only adapter; no holdings/fundamentals/news/earnings/options ingestion); 03a — Feature Engineering (no fundamentals, holdings, news, earnings, options, individual stocks, autonomous research agents, or commercial features in any feature definition); 03b — Target Generation (no fundamentals, holdings, news, earnings, options, individual stocks, autonomous research agents, or commercial features in any target definition; the closed-set target-family enumeration in `config/targets.yaml` excludes all out-of-scope inputs); also touched by 05 (paper-only enforcement) and 06 (UI read-only) | Cross-cutting; enforced architecturally by the absence of broker SDKs and the structural exclusion of out-of-scope target inputs from `targets/` | n/a at the architectural level; `config/targets.yaml` family enumeration is closed-set and excludes out-of-scope inputs (Section 3b) | No-live-broker boundary test (multi-part); secrets-in-diff check; `config/features.yaml` validation tests reject unknown family keys (Section 3a test 8.2 #24); **`config/targets.yaml` validation tests reject unknown family keys (Section 3b test 8.2 #24)** | Approver per Approval Matrix | in spec |
| Decision 2 — Data Provider and Provider-Switching Strategy | 01 — Architecture Overview (provider-abstraction boundary); 02 — Data Layer (provider interface methods, EODHD implementation, ingestion persistence with chunk-level atomicity, 30-day purge, raw payload secrets redaction); 03a — Feature Engineering (no `features/` module imports from `providers/` or any provider-specific library; adjusted-close is the canonical research price; no use of raw OHLCV in any feature); 03b — Target Generation (no `targets/` module imports from `providers/` or any provider-specific library; all target inputs flow through Postgres; adjusted-close is the canonical research price; no use of raw OHLCV in any target calculation) | `providers/`, `providers/eodhd/`, `ingestion/`, `common/db_migrations.py`, `prices.etf_prices_daily`, `prices.corporate_actions`, `ops.provider_raw_payloads`, `ops.data_quality_exceptions`, `universe.etf_provider_mapping` (Section 2 schema); `features/` calculator modules (Section 3a); `targets/` calculator modules (Section 3b) | `config/universe.yaml` (per-ETF provider mapping); `config/features.yaml` (Section 3a); `config/targets.yaml` (Section 3b) | Provider abstraction import-boundary test (Section 1); provider interface signature tests (Section 2); EODHD provider tests (Section 2); ProviderBatchResult shape and per-symbol failure collection tests (Section 2); ingestion idempotency tests (Section 2); chunk-level atomicity test (Section 2); partial-run status test (Section 2); failed-run status test (Section 2); conflicting-provider-data fail-severity test (Section 2); 30-day deletion test (Section 2); raw payload secrets redaction test (Section 2); ingestion failure-mode tests (Section 2); no-provider-import test for `features/` (Section 3a test 8.2 #18); adjusted-close-only test for `features/` (Section 3a test 8.2 #19); **no-provider-import test for `targets/` (Section 3b test 8.2 #18); adjusted-close-only test for `targets/` (Section 3b test 8.2 #19)** | Approver per Approval Matrix | in spec |
| Decision 3 — ETF Universe and Eligibility Rules | 02 — Data Layer (universe-layer model in schema and YAML; eligibility rules baseline; ETF-to-layer assignment; data-freshness thresholds in YAML); 03a — Feature Engineering (eligibility-row omission contract: `features.feature_values` rows are written only for ETF/date pairs that are rank-eligible per `universe.etf_eligibility_history` as of signal date `T`); 03b — Target Generation (parallel eligibility-row omission contract on `targets.target_values`: rows are written only for ETF/date pairs that are rank-eligible at signal date `T`, lie within ETF lifecycle bounds, are not delisted before the required `exit_date`, and whose required forward window does not exceed the snapshot's `price_table_max_as_of_date`; the full Bucket 1 absence-condition list is canonical in §6.5) | `universe.etfs`, `universe.etf_eligibility_history`, `universe.sleeves`, `universe.benchmarks` schema (Section 2 migration `0001_*`); `ingestion/` universe loader; `common/` config loader; `features/` calculator modules consuming the as-of-date eligibility view (Section 3a); **`targets/` calculator modules consuming the as-of-date eligibility view and the snapshot's `price_table_max_as_of_date` (Section 3b)** | `config/universe.yaml` (incl. `data_freshness:` block, `disclosures.current_survivor_label`); `config/features.yaml` (Section 3a); `config/targets.yaml` (Section 3b) | `config/universe.yaml` validation tests (Section 2); universe-layer enforcement test (Section 2); eligibility-rules check tests (Section 2); seeded sleeves and benchmarks present after initial migration (Section 2); survivorship-and-lifecycle test 3(c) per feature family asserting no `features.feature_values` row is written for ineligible pairs (Section 3a test 8.1 #3); **survivorship-and-lifecycle tests per target family asserting no `targets.target_values` row is written for ineligible pairs (Section 3b test 8.1 #3); forward-window observability gate test asserting no row written when the required forward window exceeds snapshot coverage (Section 3b test 8.1 #5)** | Approver per Approval Matrix | in spec |
| Decision 4 — Universe Survivorship and ETF Launch-Date Handling | 02 — Data Layer (universe-side schema, append-only eligibility history, current-survivor disclosure label storage, no-overlap constraints, canonical/denormalized field consistency rules); 03a — Feature Engineering (no `features.feature_values` row written for `(e, T)` where `T < first_traded_date(e)` or `T >= delisted_date(e)`); 03b — Target Generation (parallel: no `targets.target_values` row written for `(e, T)` where `T < first_traded_date(e)`, `T >= delisted_date(e)`, or where the ETF is delisted before the required `exit_date(e, T, h)` per the Approver-resolved `forward_window.delisted_within_window_rule = "no_row"`; replacement-ETF semantics not consulted by 03b — replacement remains a Section 5 portfolio concern); also touched by Sections 4 and 6 for downstream enforcement and disclosure surfacing | `universe.etfs` lifecycle columns; `universe.etf_eligibility_history` (append-only, EXCLUDE constraint); `universe.etf_ticker_history` (EXCLUDE constraint); `universe.etf_provider_mapping` (EXCLUDE constraint); `ingestion/` enforcement; `common.get_current_survivor_label()` (Section 2); `features/` calculator modules consulting `first_traded_date` and `delisted_date` (Section 3a); **`targets/` calculator modules consulting `first_traded_date` and `delisted_date` for both signal-date floor and forward-window-end checks (Section 3b)** | `config/universe.yaml` (lifecycle fields seeded in initial config; `disclosures.current_survivor_label` string); **`config/targets.yaml` (`forward_window.delisted_within_window_rule = "no_row"`)** | Point-in-time reconstruction tests (Section 2); no-overlap constraint tests (Section 2); canonical/denormalized field consistency tests (Section 2); rank-eligibility-before-`eligible_start_date` test (Section 2); ticker-change-via-`etf_id`-join test (Section 2); current-survivor disclosure label retrieval test (Section 2; semantic verification in Sections 4 and 6); survivorship-and-lifecycle tests 3(a) and 3(b) per feature family asserting no `features.feature_values` row is written before `first_traded_date` or on/after `delisted_date` (Section 3a test 8.1 #3); **survivorship-and-lifecycle tests per target family asserting no `targets.target_values` row is written before `first_traded_date`, on/after `delisted_date`, or when ETF delists before required `exit_date` (Section 3b test 8.1 #3 and #6)** | Approver per Approval Matrix | in spec |
| Decision 5 — Benchmark, Sleeve, and Diversifier Treatment | 02 — Data Layer (universe-side schema fields and seeded sleeves/benchmarks); 03a — Feature Engineering (benchmark-relative excess-return feature family — Family 4 — using `universe.etfs.primary_benchmark_id`; interim ETF-backed-benchmark constraint with `severity='warning'` `features.feature_run_issues` row on index-only-benchmark conditions); 03b — Target Generation (regression excess-return target family using the same `primary_benchmark_id` resolution; inherited interim ETF-backed-benchmark constraint with `severity='warning'` `targets.target_run_issues` row on index-only-benchmark conditions; **regression target row is written with `target_value = NULL`** in the index-only case, **and the classification target row is also written with `target_value = NULL`** by null propagation; **no silent benchmark substitution; no fallback to `secondary_benchmark_id`** in current 03b scope; sleeve assignments read but not modified by 03b; sleeve-conditional target families deferred to future 03b amendments); 03c — Model Layer (sleeve-aware ranking math, allowed values and semantics for `rank_method`, closing the `pending_section_3` sentinel obligation; **pending**) | `universe.etfs` (sleeve and benchmark columns); `universe.sleeves`; `universe.benchmarks` (Section 2 migration `0001_*`); `features/` benchmark-relative calculator (Section 3a); **`targets/` regression excess-return calculator (Section 3b)**; ranking math and `rank_method` validation modules (03c, **pending**) | `config/universe.yaml` (sleeve and benchmark definitions; per-ETF assignments; `rank_method` string per ETF); `config/features.yaml` (`benchmark_relative.benchmark_field: primary_benchmark_id`; `benchmark_relative.require_etf_backed_benchmark: true`); **`config/targets.yaml` (`regression_excess_return.benchmark_field: primary_benchmark_id`; `regression_excess_return.require_etf_backed_benchmark: true`)** | `config/universe.yaml` validation tests (Section 2); seeded sleeves and benchmarks present after initial migration (Section 2); per-ETF sleeve and benchmark assignment validation (Section 2); `rank_method` presence-and-type validation (Section 2); Family 4 known-input / known-output, T-1 alignment, ETF-backed-benchmark resolution, index-only-benchmark behavior (two-part: no silent substitution + `feature_run_issues` warning row), no-fallback-to-secondary-benchmark, non-overlapping trading history tests (Section 3a tests 8.1 Family 4 #10–#14); **regression excess-return known-input / known-output, forward Convention B alignment, ETF-backed-benchmark resolution, index-only-benchmark behavior (two-part: row written with `target_value = NULL` + `target_run_issues` warning row), no-fallback-to-secondary-benchmark, forward cross-series alignment for excess-return targets tests (Section 3b tests 8.1 #1–#7 and 8.2 #33)**; sleeve-aware ranking math tests (03c, **pending**) | Approver per Approval Matrix | in spec (universe-side, benchmark-relative feature side, **and benchmark-relative target side**); pending (ranking math, rank_method values) |
| Decision 6 — Target design (regression + calibrated probability) and combined score philosophy | 03a — Feature Engineering (lookback windows aligned to support 63 and 126 forward-trading-day target horizons; 03a does not generate target labels); **03b — Target Generation cashes out the regression excess-return and classification outperformance families at exactly 63 and 126 trading-day horizons (Approver-resolved per §11.6 #8 and §11.6 #9), the entry/exit Convention B (§11.6 #1, entry `T+1`, exit `T+1+h`), the classification threshold `θ = 0.0` with strict positive excess return (§11.6 #2), the missing-forward-data rule `null_on_any_missing_input_in_window` (§11.6 #3), the ETF-delisted-within-window rule `no_row` (§11.6 #4), the snapshot-coverage / front-edge-truncation absence pattern (§11.6 #10), the target persistence model with `data_snapshot_id` linkage (§11.6 #5), the index-only benchmark interim behavior (§11.6 #6), the forward-data-only labeling, and the per-row window metadata (`signal_date`, `horizon_trading_days`, `entry_date`, `exit_date`) Section 4's purge/embargo logic consumes**; 03c — Model Layer (combined-score formula, dual-target framework integration into ranking, **pending**) | `features/` returns/momentum and benchmark-relative calculators feeding 03b's target generation (Section 3a); **`targets/` regression and classification calculators (Section 3b); `targets.target_runs`, `targets.target_definitions`, `targets.target_values`, `targets.target_run_issues` schema (Section 3b)**; `models/` baseline implementations (03c, **pending**) | `config/features.yaml` (window choices: `returns_momentum.windows_trading_days: [21, 63, 126, 252]`; `benchmark_relative.windows_trading_days: [63, 126]`); **`config/targets.yaml` (`families.regression_excess_return.horizons_trading_days: [63, 126]`; `families.classification_outperformance.horizons_trading_days: [63, 126]`; `families.classification_outperformance.threshold: 0.0`; `alignment.convention: "B"`; `forward_window.delisted_within_window_rule: "no_row"`; `missing_data.rule: "null_on_any_missing_input_in_window"`)**; `config/model.yaml` (combined-score formula, **pending in 03c**) | Family 1 and Family 4 known-input / known-output, T-1 alignment, idempotency tests at 63/126-trading-day windows (Section 3a tests 8.1); **regression and classification target known-input / known-output, forward Convention B alignment, no-T-1-leakage, idempotency at 63/126-trading-day horizons (Section 3b tests 8.1); blocked-run lifecycle tests for invalidated snapshot, failed ingestion run, partial ingestion run (Section 3b tests 8.2 #21, #22); coverage-parity-with-03a test (Section 3b test 8.2 #37)**; combined-score and dual-target tests (03c, **pending**); walk-forward harness tests (Section 4, **pending**) | Approver per Approval Matrix | **`in spec` (regression and classification target generation, label alignment, per-row window metadata, persistence with `data_snapshot_id` linkage)**; `pending` (combined-score formula, ranking integration — 03c) |
| Decision 7 — Validation method (walk-forward with 126-day purge/embargo) | 03a — Feature Engineering (T-1 trading-day alignment on every feature row supports later walk-forward harness construction without bias); **03b — Target Generation (per-row window metadata `signal_date`, `horizon_trading_days`, `entry_date`, `exit_date` recorded on every `targets.target_values` row so Section 4's walk-forward harness applies purge/embargo at train/test boundaries without re-deriving windows from horizon arithmetic; overlapping-label structure recorded explicitly via the per-row window columns; 03b does NOT apply purge/embargo at the target-emission level — Section 4 owns that)**; 03c — Model Layer (calibration pipeline per the SDR's named methods, **pending**); 04 — Backtest / Attribution / Validation (walk-forward harness, purge/embargo enforcement, leakage tests, **pending**) | `features/` calculator modules establishing T-1 alignment (Section 3a); **`targets/` calculator modules establishing forward Convention B alignment and recording per-row window metadata (Section 3b)**; `models/` calibration modules (03c, **pending**); `backtest/`, `attribution/` modules (Section 4, **pending**) | `config/features.yaml` (T-1 alignment is structural, not parameterized); `config/targets.yaml` (forward Convention B alignment is structural; horizons parameterized at 63 and 126 trading days); `config/model.yaml` (calibration method choice, **pending in 03c**); backtest-window and validation parameters (Section 4, **pending**) | T-1 trading-day alignment tests per feature family — weekend boundary, holiday boundary, cross-series alignment for benchmark-relative features (Section 3a tests 8.2 #31, #32, #33); **forward Convention B alignment tests per target family — weekend boundary, holiday boundary, forward cross-series alignment for excess-return targets (Section 3b tests 8.2 #31, #32, #33); adjacent-target-rows-with-overlapping-forward-windows expected (Section 3b §9 edge case 16)**; calibration tests (03c, **pending**); walk-forward harness, purge/embargo, leakage tests (Section 4, **pending**) | Approver per Approval Matrix | pending (03c calibration, Section 4 harness); 03b row text extended on the per-row-window-metadata-supplied-to-Section-4 contribution |
| Decision 8 — Transaction cost model (4-bucket bps defaults) | TBD | TBD | `config/costs.yaml` | TBD | Approver per Approval Matrix | pending |
| Decision 9 — Regime taxonomy (SPY-vs-200dma, VIX percentile + realized-vol fallback) | 03a — Feature Engineering (one feature-side regime primitive defined: `regime_spy_above_sma_200` (Family 5), default off in `config/features.yaml`; activation requires explicit Approver direction and materialization as repeated rows for each eligible ETF/date with real `etf_id` values per §6.2.5; **the full `regime/` subpackage, `regime.*` schema, and SDR Decision 9 reporting layer remain outside 03a**); Section 3 (regime computation, **pending**); Section 4 (regime consumption / reporting layer, **pending**) | `features/` regime-primitive calculator (Section 3a, default off); `regime/` subpackage (Section 1 reservation; Section 3 computation, **pending**); regime reporting modules (Section 4, **pending**) | `config/features.yaml` (`regime_primitives.spy_vs_200dma.enabled: false` default); `config/regime.yaml` (regime-classification thresholds, **pending in Section 3 / 4**) | Family 5 known-input / known-output, T-1 alignment, disabled-by-default tests (Section 3a tests 8.1 Family 5 #15–#17, exercised only when enabled); full regime classification and reporting tests (Section 3 / Section 4, **pending**) | Approver per Approval Matrix | pending (Sections 3 and 4); Section 3a contributes a feature-side primitive only (default off) |
| Decision 10 — Portfolio Management and Risk Rules | TBD | TBD | TBD (Section 5; likely `config/portfolio.yaml`) | TBD | Approver per Approval Matrix | pending |
| Decision 11 — Model Tracking, Attribution, Data Quality, and Lightweight MLOps | 01 — Architecture Overview (Postgres-as-system-of-record boundary; MLflow-as-tracking architecture); 02 — Data Layer (data-quality framework with framework-level enforcement of forbidden auto-resolutions; `ingestion/` is the only emitter of `ops.data_quality_exceptions` rows in Phase 1; framework remains ingestion-owned); 03a — Feature Engineering (`features.feature_runs.data_snapshot_id` linkage; `features.feature_values.feature_run_id` row-level traceability via composite FK; `features.feature_run_issues` as the feature-layer issue log with closed `issue_type` and `severity` enumerations and the open-run-before-validation lifecycle); 03b — Target Generation (`targets.target_runs.data_snapshot_id` linkage; `targets.target_values.target_run_id` row-level traceability via composite FK; `targets.target_run_issues` as the target-layer issue log with closed `issue_type` and `severity` enumerations and the open-run-before-validation lifecycle (the closed `issue_type` enumeration does **not** include a `forward_window`-related value in current 03b scope per §11.6 #10); database-level `target_set_version` integrity via UNIQUE on `target_runs(target_run_id, target_set_version)` plus composite FKs from `target_values`); **`ops.data_quality_exceptions` remains ingestion-owned and unmodified by 03b** per Section 2 v1.0 LOCKED; 03c — Model Layer (MLflow client-side integration on writer side, linking MLflow runs back to both `feature_run_id` and `target_run_id`, **pending**); 04 — Backtest / Attribution (attribution storage; data-snapshot consumption, **pending**) | `common/` (db helpers, mlflow helpers); `ingestion/` (data-quality emitter); `ops.data_quality_exceptions`, `ops.ingestion_runs`, `ops.provider_raw_payloads`, `ops.data_snapshots`, `ops.schema_migrations` (Section 2 schema); `scripts/postgres-init/`; `features.feature_runs`, `features.feature_definitions`, `features.feature_values`, `features.feature_run_issues` (Section 3a schema); **`targets.target_runs`, `targets.target_definitions`, `targets.target_values`, `targets.target_run_issues` (Section 3b schema)**; `models/`, `backtest/`, `attribution/` MLflow writers (later-section, **pending**); `ui/` Model Registry / Run Browser (Section 6 reader, **pending**) | n/a at the architectural level; `.env` for MLflow connection (Section 2); `config/features.yaml` (Section 3a); **`config/targets.yaml` (Section 3b — `target_set_version` recorded on every run)** | Postgres-as-system-of-record import test (Section 1); cross-container connectivity smoke tests (Section 1); database/role bootstrap test (Section 2); migration-runner refusal of `CREATE DATABASE` / `CREATE ROLE` test (Section 2); data-quality framework tests covering nine-field exception report and forbidden auto-resolutions (Section 2); ingestion-run metadata test (Section 2); data snapshot creation and invalidation tests (Section 2); `data_snapshot_id` linkage test, invalidated-snapshot rejection test, ingestion-run consumption test, failed-run exclusion test, `feature_set_version` consistency test (database-level), composite FK to `feature_definitions` test, no-write-to-`ops.*` static check, `features.feature_run_issues` write test, `features.feature_run_issues` query test, cross-run idempotency test (Section 3a tests 8.2); **parallel `targets.*` tests: `data_snapshot_id` linkage test, invalidated-snapshot rejection test, ingestion-run consumption test, failed-run exclusion test, `target_set_version` consistency test (database-level), composite FK to `target_definitions` test, no-write-to-`ops.*` static check, `targets.target_run_issues` write test, `targets.target_run_issues` query test, cross-run idempotency test (Section 3b tests 8.2)** | Approver per Approval Matrix | in spec |
| Decision 12 — Two-gate model promotion + multi-condition kill switch | TBD | TBD | `config/model.yaml`, `config/portfolio.yaml` | TBD | Approver per Approval Matrix | pending |
| Decision 13 — LLM advisory use during build | n/a | n/a | n/a | n/a | n/a (governed by Engineering Workflow) | n/a |
| Decision 14 — Danelfin timing (deferred until baseline exists) | TBD | TBD | TBD | TBD | Approver per Approval Matrix | pending |
| Decision 15 — Account/broker strategy (paper-only Phase 1, broker-neutral) | TBD | TBD | `config/portfolio.yaml` | TBD | Approver per Approval Matrix | pending |
| Decision 16 — Phase 1 Success Criteria and Bias Controls | 01 — Architecture Overview (architectural reservation only); 02 — Data Layer (universe time-awareness foundation, data-layer-level look-ahead and survivorship checks, data-snapshot foundation for reproducibility); 03a — Feature Engineering (T-1 trading-day alignment on every feature row including weekend / holiday / cross-series boundaries; survivorship gating producing absence of rows for ineligible / out-of-lifecycle ETF/date pairs; reproducibility via `data_snapshot_id` and `feature_run_id`; idempotency across `'succeeded'` runs; index-only-benchmark interim constraint preserves bias controls by refusing silent substitution); **03b — Target Generation (forward-data-only labeling on every target row via Convention B; survivorship gating producing absence of rows for ineligible / out-of-lifecycle ETF/date pairs and for ETFs delisted before required `exit_date`; row-level forward-window observability via the snapshot's `price_table_max_as_of_date` producing absence — not a failed run — for front-edge truncation; reproducibility via `targets.target_runs.data_snapshot_id` and `targets.target_values.target_run_id`; idempotency across `'succeeded'` runs sharing the same `data_snapshot_id` / `target_set_version` / `commit_hash` / `config_hash`; per-row window metadata (`signal_date`, `horizon_trading_days`, `entry_date`, `exit_date`) supporting Section 4 purge/embargo enforcement; index-only-benchmark interim constraint preserves bias controls by refusing silent substitution and writing rows with `target_value = NULL` rather than fabricating values)**; 04 — Backtest / Attribution (purge/embargo enforcement, leakage tests, reproducibility metadata, regime reporting, **pending**); 06 — UI (surface bias-control disclosures and labels, **pending**) | Cross-cutting; Section 2 owns `universe.etf_eligibility_history`, lifecycle columns, `prices.etf_prices_daily.adjusted_close` convention, `disclosures.current_survivor_label`, `ops.data_snapshots`, data-layer look-ahead checks; Section 3a owns the `features.*` schema and the feature-level T-1 / survivorship / reproducibility / idempotency tests; **Section 3b owns the `targets.*` schema (`target_runs`, `target_definitions`, `target_values`, `target_run_issues`) and the target-level forward-Convention-B / survivorship / forward-window-observability / reproducibility / idempotency tests**; Sections 4 and 6 own their respective downstream contributions | Cross-cutting; Section 2 owns `config/universe.yaml`; Section 3a owns `config/features.yaml` (closed-set family validation and `feature_set_version` discipline); **Section 3b owns `config/targets.yaml` (closed-set family validation and `target_set_version` discipline)** | Section 1 introduces no test for invariant 7. Section 2 introduces: point-in-time reconstruction tests; canonical/denormalized field consistency tests; no-overlap constraint tests on the three effective-dated tables; rank-eligibility-before-`eligible_start_date` test; current-survivor disclosure label retrieval test; data-layer look-ahead checks; data snapshot creation and invalidation tests. Section 3a introduces: per-feature-family T-1 alignment tests including weekend, holiday, and cross-series boundary tests; per-feature-family survivorship-and-lifecycle tests asserting absence of rows for ineligible / out-of-lifecycle pairs; per-feature-family idempotency tests; `data_snapshot_id` linkage tests; invalidated-snapshot rejection test; ingestion-run consumption tests for failed and partial dependencies; cross-run idempotency test; `feature_set_version` database-level consistency test; composite FK enforcement tests; `features.feature_run_issues` write and query tests (Section 3a tests 8.1 and 8.2). **Section 3b introduces: per-target-family forward Convention B alignment tests including weekend, holiday, and forward-cross-series boundary tests (Section 3b tests 8.2 #31, #32, #33); per-target-family survivorship-and-lifecycle tests including delisted-before-`exit_date` (Section 3b test 8.1 #3 and #6); per-target-family forward-window-observability tests asserting absence — not failed run — for front-edge truncation (Section 3b test 8.1 #5 and 8.2 #37); per-target-family idempotency tests; `data_snapshot_id` linkage tests; invalidated-snapshot rejection test; ingestion-run consumption tests for failed and partial dependencies; cross-run idempotency test; `target_set_version` database-level consistency test; composite FK enforcement tests; `targets.target_run_issues` write and query tests; coverage-parity-with-03a test (Section 3b tests 8.1 and 8.2).** Section 4 introduces purge/embargo and leakage tests, **pending**. | Approver per Approval Matrix | in spec |
| Decision 17 — Phase 1 operator UI (Dash) on Linux/Hostinger VPS | 01 — Architecture Overview (UI as read-only architectural area, hosted inside the application container; UI read-only invariant covers both Python imports and database-access paths); 06 — UI Layer (seven Phase 1 screens, content, and the database-access enforcement mechanism for the read-only constraint) | `ui/` | TBD (Section 6 will specify; UI screens read from existing `config/*.yaml` and may add UI-only config) | UI read-only import-boundary test (Section 1); UI database-access enforcement test — no INSERT/UPDATE/DELETE/DDL from `ui/` (Section 6); UI smoke tests confirming pages load (Section 6); UI cannot bypass approval gates or modify SDR-governed state (Section 6) | Approver per Approval Matrix | in spec |
| Decision 18 — Container architecture (Docker, multi-container stack) | 01 — Architecture Overview | `Dockerfile`, `docker-compose.yml`, `.env.example`, `.dockerignore`, `scripts/backup.sh`, `scripts/restore.sh` | n/a (these are deployment artifacts, not application config) | Container startup smoke test (three-container Phase 1 stack; cron daemon health-checked alongside Dash so a Dash-only signal cannot mask cron failure); cross-container connectivity smoke tests; image rebuild test from clean cache; portability test on a fresh test environment; `.env.example` parity check against `docker-compose.yml` env references and `common/` credential helper usage | Approver per Approval Matrix | in spec |

---

## Notes

**Document version history.**
- v0.1 stub (2026-04-26) — Initial placeholder rows; Decisions 17 and 18 marked as `*(pending SDR rev)*` because they were identified during Engineering Workflow drafting and had not yet been incorporated into the SDR.
- v0.2 (2026-04-26) — Section 1 v1.0 LOCKED merged. Replacement rows applied for Decisions 1, 2, 11, 16, 17, 18. The `*(pending SDR rev)*` parentheticals on 17 and 18 are removed because the SDR is locked at v1.0 with both decisions final. Decision 16 receives an architectural footprint for the first time, reflecting Section 1's time-aware research auditability invariant. Sections 2–6 remain to be drafted; their rows remain `pending` until their owning section is finalized.
- v0.3 (2026-04-27) — Section 2 v1.0 LOCKED merged. Replacement rows applied for Decisions 2, 3, 4, 5, 11, and 16. Decision 10 label corrected to "Portfolio Management and Risk Rules" while remaining pending until Section 5. Sections 3–6 remain pending.
- v0.4 (2026-04-29) — Section 3a v1.0 LOCKED / APPROVED merged per the Section 3a traceability companion artifact (`docs/reviews/2026-04-29_spec_03a_feature_engineering_traceability_updates.md`). Replacement rows applied for Decisions 1, 2, 3, 4, 5, 6, 7, 9, 11, and 16. Decisions 1, 2, 3, 4, 11, and 16 retain `in spec` status with row text extended to record 03a's specific contributions. Decision 5 status evolves from `in spec (universe-side); pending (ranking math)` to `in spec (universe-side and benchmark-relative feature side); pending (ranking math, rank_method values)`. Decisions 6, 7, and 9 retain overall `pending` status with row text extended to record 03a's preparatory contributions and the still-pending downstream work in 03b, 03c, Section 3 (regime computation), and Section 4. Decisions 8, 10, 12, 13, 14, 15, 17, and 18 are unchanged. **Project-artifacts cross-reference disposition:** the matrix does not currently include a structured project-artifacts cross-reference, so the `features.*` schema (`features.feature_runs`, `features.feature_definitions`, `features.feature_values`, `features.feature_run_issues`) and `config/features.yaml` are recorded in the Module(s) and Config File(s) cells of the relevant decision rows above (notably Decisions 2, 3, 5, 6, 9, 11, 16) rather than in a separate cross-reference list. If a structured cross-reference is added in a future matrix version, these artifacts should be migrated into it. Sections 3b, 3c, 4, 5, 6 remain pending.
- v0.5 (2026-04-29) — Section 3b v1.0 LOCKED / APPROVED merged per the Section 3b traceability companion artifact (`docs/reviews/2026-04-29_spec_03b_target_generation_traceability_updates.md`). Replacement rows applied for Decisions 1, 2, 3, 4, 5, 6, 7, 11, and 16. Decisions 1, 2, 3, 4, 11, and 16 retain `in spec` status with row text extended to record 03b's specific contributions. Decision 5 status form evolves from `in spec (universe-side and benchmark-relative feature side); pending (ranking math, rank_method values)` to `in spec (universe-side, benchmark-relative feature side, and benchmark-relative target side); pending (ranking math, rank_method values)`. Decision 6 transitions further within its existing split status: target generation at 63/126-day horizons, label alignment, persistence with `data_snapshot_id` linkage, and per-row window metadata are now `in spec`; combined-score formula and ranking integration remain `pending` in 03c. Decision 7 row text extended to record 03b's per-row window metadata (`signal_date`, `horizon_trading_days`, `entry_date`, `exit_date`) supplied to Section 4; overall status remains `pending` (03c calibration, Section 4 harness). Decisions 8, 9, 10, 12, 13, 14, 15, 17, and 18 not touched by 03b and retain their post-Section-3a-lock rows. **Label-preservation cleanup at merge time:** the Section 3b companion artifact's *Replacement rows* table used the row labels `Decision 6 — Target Design and Ranking Objective` and `Decision 7 — Validation, Calibration, and Backtest Confidence Level`, which differ from the v0.4 matrix labels `Decision 6 — Target design (regression + calibrated probability) and combined score philosophy` and `Decision 7 — Validation method (walk-forward with 126-day purge/embargo)`. The companion's own *Audit findings* section explicitly states "No new label findings were identified", so per the companion's stated intent the v0.4 labels are preserved verbatim and only the row content (Spec Section(s), Module(s), Config File(s), Required Tests, Approval Gate, Status) is replaced. This is recorded here for traceability; no separate matrix amendment is required. **Project-artifacts cross-reference disposition (continued from v0.4):** the matrix still does not include a structured project-artifacts cross-reference, so the `targets.*` schema (`targets.target_runs`, `targets.target_definitions`, `targets.target_values`, `targets.target_run_issues`) and `config/targets.yaml` are recorded in the Module(s) and Config File(s) cells of the relevant decision rows above (notably Decisions 2, 3, 4, 5, 6, 11, 16) rather than in a separate cross-reference list. **No Section 1 amendment is proposed by Section 3b** for the introduction of `config/targets.yaml`; the Section 1 *Config dependencies* table is not updated by this merge. Sections 3c, 4, 5, 6 remain pending. Approval note: `docs/reviews/2026-04-29_spec_03b_target_generation_approval.md`.

**Decision 13 (LLM advisory use).** This is a process commitment, not a strategy commitment that generates code. It is governed by the Engineering Workflow rather than implemented as modules, so it is marked `n/a` for spec section, modules, config, and tests. Listed in the matrix for completeness.

**Decision 16 (Phase 1 success criteria and bias controls).** Cross-cutting. Section 1 establishes the architectural reservation (the time-aware research auditability invariant) without defining schema fields or implementation. Sections 2, 3, and 4 own the actual data contracts, alignment rules, purge/embargo logic, and tests. Sections 2 and 6 also carry disclosure-side responsibilities (current-survivor labels on early outputs and the UI surfacing of bias-control disclosures).

**`in spec` status interpretation.** A row marked `in spec` means the relevant spec section has been drafted and may be approved. As of v0.5 of this matrix, Sections 1, 2, 3a, and 3b are v1.0 LOCKED / APPROVED; Sections 3c, 4, 5, and 6 remain pending unless otherwise noted. When the corresponding modules begin implementation, status moves to `in build`. When modules are merged and integrated, status moves to `live`.

**TBD entries.** Entries marked `TBD` will be filled in as each Engineering Specification section is drafted and approved. The "Spec Section(s)" column is filled first (when the relevant section is started), followed by Module(s), Config File(s), and Required Tests as the section is fleshed out. Status moves from `pending` to `in spec` to `in build` to `live` over time.

**One row per decision is the default.** If a single SDR decision requires implementation across multiple spec sections or many modules, expand the row's Spec Section(s) and Module(s) columns to comma-separated lists, or split the row into sub-rows with shared decision number (e.g., "Decision 11 — MLOps storage" and "Decision 11 — MLOps attribution").

**Approval Gate column.** For Phase 1, every entry routes through the Approver via the Engineering Workflow Approval Matrix (Section 2.3). The column exists to make the gate explicit per row and to support future phases that may introduce additional approval gates (e.g., a separate broker compliance gate if live trading is ever introduced).

---

**End of document.**
